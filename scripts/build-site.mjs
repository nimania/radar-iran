import fs from "node:fs/promises"; let data={generated_at:null,count:0,products:[]}; try{data=JSON.parse(await fs.readFile("data/products/latest.json","utf8"))}catch{}
const money=n=>n==null?"—":new Intl.NumberFormat("fa-IR").format(n)+" تومان";
const rows=(data.products||[]).slice(0,40).map(p=>`<tr><td><a href="${p.url||"#"}" target="_blank" rel="noreferrer">${p.title||"بدون عنوان"}</a><small>${p.seller||""}</small></td><td>${p.category_label||"—"}</td><td>${money(p.price)}</td><td>${p.discount_percent?p.discount_percent+"٪":"—"}</td><td>${p.rating??"—"}</td><td>${p.rank??"—"}</td></tr>`).join("");
const html=`<!doctype html><html lang="fa" dir="rtl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Radar Iran — نبض بازار مصرف ایران</title><style>*{box-sizing:border-box}body{font-family:system-ui,-apple-system,sans-serif;max-width:1280px;margin:auto;padding:28px;background:#0a0c0f;color:#f4f4f5}header{padding:7vh 0 4vh}h1{font-size:clamp(3rem,9vw,7rem);margin:0;letter-spacing:-.05em}h1 b{color:#ff3b30}p{color:#a1a1aa}.stats{display:flex;gap:12px;flex-wrap:wrap;margin:24px 0}.stat{border:1px solid #292d35;background:#12151a;border-radius:16px;padding:16px 20px;min-width:180px}.stat strong{font-size:1.6rem;display:block}table{width:100%;border-collapse:collapse;background:#12151a;border:1px solid #292d35;border-radius:18px;overflow:hidden}th,td{text-align:right;padding:14px;border-bottom:1px solid #292d35}th{color:#a1a1aa;font-size:.8rem}a{color:#fff;text-decoration:none}td small{display:block;color:#71717a;margin-top:5px}@media(max-width:760px){th:nth-child(2),td:nth-child(2),th:nth-child(5),td:nth-child(5){display:none}table{font-size:.82rem}}</style></head><body><header><small>نبض بازار مصرف ایران</small><h1>RADAR <b>IRAN</b></h1><p>رصد مستقل قیمت، جایگاه و سیگنال‌های بازار آنلاین ایران</p></header><section class="stats"><div class="stat"><small>محصولات این مشاهده</small><strong>${data.count||0}</strong></div><div class="stat"><small>آخرین بروزرسانی</small><strong style="font-size:1rem">${data.generated_at?new Date(data.generated_at).toLocaleString("fa-IR"):"—"}</strong></div></section><table><thead><tr><th>محصول</th><th>دسته</th><th>قیمت</th><th>تخفیف</th><th>امتیاز</th><th>جایگاه مشاهده</th></tr></thead><tbody>${rows||'<tr><td colspan="6">دادهٔ نرمال‌شده هنوز در دسترس نیست؛ Collector باید یک بار دیگر اجرا شود.</td></tr>'}</tbody></table></body></html>`;
await fs.mkdir("dist",{recursive:true});await fs.writeFile("dist/index.html",html);