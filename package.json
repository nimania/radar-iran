{
  "name": "radar-iran",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "collect": "node scripts/collect.mjs",
    "collect:used": "node scripts/collect-used.mjs",
    "collect:all": "npm run collect && npm run collect:used",
    "build": "node scripts/build-site.mjs",
    "check": "node scripts/check.mjs",
    "service:check": "node --check service/worker.js"
  },
  "engines": {
    "node": ">=20"
  }
}
